<?php
include 'class.Database.inc';
class Admin {
  protected $username;
  protected $password;
  protected $user_id;

  public function register_Patient($doctor_id, $password) {
    $db = Database::getInstance();
  }
}

?>
